'use strict';

/**
 * volunteer-meeting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::volunteer-meeting.volunteer-meeting');
